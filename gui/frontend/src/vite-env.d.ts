// FIX(titlebar): Vite client types — provides `*.png` (and other asset)
// module declarations so `import appIcon from "../assets/app.png"`
// typechecks under `tsc --noEmit`.
/// <reference types="vite/client" />
